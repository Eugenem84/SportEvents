# LEGACY: контур на Beget-VPS (`159.194.252.9`) выведен из эксплуатации, сервер признан
# недоверенным. Актуальный контур — домашний сервер: TLS терминирует общий Caddy дома
# (`home-server-vps/server/caddy/Caddyfile`), регламент — DEPLOY.md.

# Caddy автоматически выпустит и продлит HTTPS-сертификат Let's Encrypt
# для указанного домена (на VPS будут открыты порты 80 и 443 для проверки владения).
# Никаких ручных действий с сертификатом не нужно。



sport-events.dev.medovf2h.beget.tech {
	reverse_proxy app:8080
}